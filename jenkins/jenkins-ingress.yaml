apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: default # Assuming Jenkins is in its own namespace
spec:
  ingressClassName: traefik # Specify your Ingress Controller class
  rules:
  - host: "jenkins.softsurve.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins-operator-http-example # The name of your Jenkins Service
            port:
              number: 8080 # The port the Jenkins Service exposes
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: default # Assuming Jenkins is in its own namespace
spec:
  ingressClassName: traefik # Specify your Ingress Controller class
  rules:
  - host: "jenkins.fairmerce.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins-operator-http-example # The name of your Jenkins Service
            port:
              number: 8080 # The port the Jenkins Service exposes

